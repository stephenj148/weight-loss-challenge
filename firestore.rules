rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Temporary permissive rules for testing - ALLOW ALL authenticated users to write
    // TODO: Restrict this after testing is complete
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}